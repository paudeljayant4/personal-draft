export const Mindset = () => {
  return (
    <section className="relative px-6 py-20 max-w-2xl mx-auto text-center">
      <div className="absolute -top-10 left-0 w-full h-20 bg-vermilion/5"></div>
      <blockquote className="text-3xl font-semibold italic text-foreground">
        “The world doesn't wait for excuses. It only sees results.”
      </blockquote>
    </section>
  );
};